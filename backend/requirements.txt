# FastAPI & Server
fastapi==0.109.0
# mcp 2.x requires uvicorn>=0.31.1
uvicorn[standard]>=0.31.1
# fastapi-users 15.0.2 requires python-multipart==0.0.21
python-multipart==0.0.21

# Authentication
# 15.0.2 pins pyjwt==2.10.1 (CVE-2025-68481 fix). workos 6+ needs pyjwt~=2.12,
# so stay on workos 5.22–5.x which has workos.session and accepts PyJWT 2.10.
fastapi-users[sqlalchemy,oauth]==15.0.2
httpx-oauth==0.14.1
workos>=5.22.0,<6.0.0

# Database
sqlalchemy[asyncio]==2.0.25
asyncpg==0.29.0
alembic==1.13.1
pgvector==0.2.4

# Google APIs
google-api-python-client==2.111.0
google-auth==2.26.1

# Configuration
# >=2.5.2 required by mcp
pydantic-settings>=2.5.2,<3
python-dotenv==1.0.0

# Templates
jinja2==3.1.3

# Security
passlib[bcrypt]==1.7.4
python-jose[cryptography]==3.3.0

# Analytics (optional)
posthog==3.3.1

# Observability - tracing embeddings and retrieval (optional)
langfuse>=3.0.0

# Redis/Celery (optional, for async jobs)
celery[redis]==5.3.6
redis==5.0.1

# Audio transcription (WhisperX: batched Whisper ASR + word-level alignment, local CPU inference)
whisperx>=3.4.0

# Video frame extraction & storage
# 2.28+ allows httpx 0.28 (older 2.6.x still caps httpx at <0.28)
supabase>=2.28.0
# mcp needs >=0.27; workos 5.22+ needs httpx>=0.28.1; supabase 2.28+ allows <0.29
httpx>=0.28,<0.29
azure-storage-blob>=12.19.0
azure-servicebus>=7.13.0

# Remote MCP server (streamable HTTP transport). 2.x required for MCP Apps UI.
mcp>=2.0.0

# Instagram reel download (keep recent; Instagram extractor churns)
yt-dlp>=2025.1.26



